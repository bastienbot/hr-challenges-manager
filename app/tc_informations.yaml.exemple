marketing-event:
  chalenge-type: default (email)
  name: template-name
buz-dev:
  chalenge-type: default
  name: template-name
csm:
  chalenge-type: default
  name: template-name
backend:
  chalenge-type: gitlab (create user / fork repo)
  name: template-name
  id: reposlug
frontend:
  chalenge-type: gitlab
  name: template-name
  id: reposlug
ds:
  chalenge-type: gitlab
  name: template-name
  id: reposlug
